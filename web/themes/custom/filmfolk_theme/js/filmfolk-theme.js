/**
 * @file
 * filmfolk_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.filmfolkTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
